/* Estilos generales */
body {
  font-family: 'Helvetica Neue', Arial, sans-serif;
  line-height: 1.6;
  margin: 0;
  padding: 0;
}

header {
  background-color: #333;
  color: #fff;
  text-align: center;
  padding: 1rem;
}

main {
  max-width: 800px;
  margin: 0 auto;
  padding: 2rem;
}

h1 {
  font-size: 2.5rem;
  font-weight: bold;
  margin-bottom: 1.5rem;
}

h2 {
  font-size: 1.8rem;
  font-weight: bold;
  margin-bottom: 1.2rem;
}

h3 {
  font-size: 1.4rem;
  font-weight: bold;
  margin-bottom: 1rem;
}

p {
  font-size: 1.2rem;
  line-height: 1.6;
  margin-bottom: 1rem;
}

ul {
  list-style: disc;
  margin-left: 2rem;
  margin-bottom: 1rem;
}

img {
  max-width: 100%;
  height: auto;
  margin-bottom: 1.5rem;
}

/* Enlaces */
a {
  color: #007BFF;
  text-decoration: none;
}

a:hover {
  text-decoration: underline;
}

/* Estilos para los módulos y lecciones */
.module {
  margin-bottom: 3rem;
}

.module h2 {
  border-bottom: 2px solid #007BFF;
  padding-bottom: 0.5rem;
}

.lesson {
  margin-left: 2rem;
}

/* Estilos para la sección de imágenes */
.image-section {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  margin-bottom: 2rem;
}

.image-section img {
  max-width: 48%;
  margin-bottom: 1rem;
}

/* Estilos para la sección de acciones */
.actions-section {
  display: flex;
  justify-content: space-between;
  margin-top: 2rem;
}

.actions-section a {
  background-color: #007BFF;
  color: #fff;
  padding: 0.5rem 1rem;
  text-decoration: none;
  border-radius: 4px;
  transition: background-color 0.3s ease;
}

.actions-section a:hover {
  background-color: #0056b3;
}

/* Agrega más estilos CSS según tus necesidades */

